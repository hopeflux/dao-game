import {
    // create naive ui
    create,
    NAlert,
    NText,
    NButton,
    NSpin,
    NCard,
    NModal,
    NInput,

} from 'naive-ui'

export default create({
    components: [
        NAlert,
        NText,
        NButton,
        NSpin,
        NCard,
        NModal,
        NInput,
    ]
})